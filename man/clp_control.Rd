% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve.R
\name{clp_control}
\alias{clp_control}
\title{Control parameters for clp_solve()}
\usage{
clp_control(
  log_level = 0L,
  algorithm = c("auto", "primal", "dual", "barrier", "barrier_nocross"),
  presolve = TRUE,
  max_iterations = NULL,
  max_seconds = NULL,
  primal_tolerance = NULL,
  dual_tolerance = NULL,
  scaling = NULL
)
}
\arguments{
\item{log_level}{How much Clp prints: 0 silent (the default), up to 4.}

\item{algorithm}{One of \code{"auto"}, \code{"primal"}, \code{"dual"},
\code{"barrier"} or \code{"barrier_nocross"}.}

\item{presolve}{Run Clp's presolve.  Ignored when \code{algorithm} is
\code{"auto"} and \code{presolve} is \code{TRUE}, which is Clp's own
default path.}

\item{max_iterations}{Iteration limit, or \code{NULL} for Clp's default.}

\item{max_seconds}{Time limit in seconds, or \code{NULL} for no limit.}

\item{primal_tolerance, dual_tolerance}{Simplex tolerances, or \code{NULL}
for Clp's defaults.}

\item{scaling}{Scaling mode: 0 off, 1 equilibrium, 2 geometric, 3 auto,
4 dynamic, or \code{NULL} for Clp's default.}
}
\value{
A list of control settings for \code{\link{clp_solve}}.
}
\description{
Control parameters for clp_solve()
}
\examples{
clp_control(algorithm = "dual", max_seconds = 10)
}
