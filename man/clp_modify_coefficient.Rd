% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lowlevel.R
\name{clp_modify_coefficient}
\alias{clp_modify_coefficient}
\title{Change one matrix coefficient}
\usage{
clp_modify_coefficient(model, row, col, value, keep_zero = TRUE)
}
\arguments{
\item{model}{A \code{"clp_model"} object.}

\item{row, col}{0-based row and column position.}

\item{value}{New coefficient.}

\item{keep_zero}{Keep the entry in the sparse structure when \code{value}
is zero.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Needs a Clp build that provides \code{Clp_modifyCoefficient} (1.18 or
later); check with \code{clp_features()}.
}
\examples{
if (isTRUE(clp_features()[["modify_coefficient"]])) {
  model <- clp_model()
  clp_load_problem(model, 1, 1, c(0L, 1L), 0L, 1, rowub = 1)
  clp_modify_coefficient(model, 0, 0, 2)
  clp_free(model)
}
}
