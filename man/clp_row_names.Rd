% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/names-io.R
\name{clp_row_names}
\alias{clp_row_names}
\alias{clp_col_names}
\alias{clp_row_name}
\alias{clp_col_name}
\alias{clp_set_names}
\alias{clp_set_row_name}
\alias{clp_set_col_name}
\alias{clp_drop_names}
\alias{clp_length_names}
\title{Row and column names}
\usage{
clp_row_names(model)

clp_col_names(model)

clp_row_name(model, index)

clp_col_name(model, index)

clp_set_names(model, row_names, col_names)

clp_set_row_name(model, index, name)

clp_set_col_name(model, index, name)

clp_drop_names(model)

clp_length_names(model)
}
\arguments{
\item{model}{A \code{"clp_model"} object.}

\item{index}{0-based row or column position.}

\item{row_names, col_names}{Character vectors with one entry per row or
column.}

\item{name}{A single name.}
}
\value{
The getters return character vectors or a single string;
  \code{clp_length_names()} an integer; the setters \code{NULL} invisibly.
}
\description{
\code{clp_row_names()} and \code{clp_col_names()} return all names as a
character vector; \code{clp_row_name()} and \code{clp_col_name()} fetch a
single one by 0-based position, as in the C API.  A model that carries no
names of its own reports the generated defaults \code{"R0000000"} and
\code{"C0000000"}; \code{clp_length_names()} is 0 in that case.
}
\details{
\code{clp_set_names()} replaces every name at once and works with any Clp
version.  \code{clp_set_row_name()} and \code{clp_set_col_name()} change a
single name but need Clp 1.18 or later; see \code{\link{clp_features}}.
}
\examples{
model <- clp_model()
clp_load_problem(model, 2, 1, c(0L, 1L, 2L), c(0L, 0L), c(1, 1), rowub = 1)
clp_set_names(model, "supply", c("x", "y"))
clp_col_names(model)
clp_free(model)
}
