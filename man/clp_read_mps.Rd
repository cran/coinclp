% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/names-io.R
\name{clp_read_mps}
\alias{clp_read_mps}
\title{Read an MPS file}
\usage{
clp_read_mps(model, file, keep_names = TRUE, ignore_errors = FALSE)
}
\arguments{
\item{model}{A \code{"clp_model"} object.}

\item{file}{Path to the MPS file.}

\item{keep_names}{Keep the row and column names from the file.}

\item{ignore_errors}{Carry on after errors in the file.}
}
\value{
Clp's integer return code, invisibly: 0 on success.
}
\description{
Reads a problem in MPS format into a model, using Clp's own reader.
Gzipped files are handled by Clp when it was built with zlib.
}
\examples{
model <- clp_model()
path <- system.file("extdata", "productmix.mps", package = "coinclp")
if (nzchar(path)) {
  clp_read_mps(model, path)
  clp_num_rows(model)
}
clp_free(model)
}
