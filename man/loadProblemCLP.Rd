% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compat-clpAPI.R
\name{loadProblemCLP}
\alias{loadProblemCLP}
\alias{loadMatrixCLP}
\title{Build a problem, clpAPI style}
\usage{
loadProblemCLP(
  lp,
  ncols,
  nrows,
  ia,
  ja,
  ra,
  lb = NULL,
  ub = NULL,
  obj_coef = NULL,
  rlb = NULL,
  rub = NULL
)

loadMatrixCLP(lp, ncols, nrows, ia, ja, ra)
}
\arguments{
\item{lp}{A \code{\linkS4class{clpPtr}} object.}

\item{ncols, nrows}{Problem dimensions.}

\item{ia}{Integer vector of column starts, length \code{ncols + 1}.}

\item{ja}{Integer vector of 0-based row indices.}

\item{ra}{Numeric vector of matrix entries.}

\item{lb, ub}{Column bounds, or \code{NULL}.}

\item{obj_coef}{Objective coefficients, or \code{NULL}.}

\item{rlb, rub}{Row bounds, or \code{NULL}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Column-major, 0-based arguments, exactly as in \pkg{clpAPI}:
\code{ia} holds the column starts, \code{ja} the row indices and
\code{ra} the matrix entries.
}
\examples{
lp <- initProbCLP()
loadProblemCLP(lp, 2, 1, c(0, 1, 2), c(0, 0), c(1, 1),
               lb = c(0, 0), ub = c(1e30, 1e30), obj_coef = c(-1, -2),
               rlb = -1e30, rub = 3)
solveInitialCLP(lp)
getObjValCLP(lp)
delProbCLP(lp)
}
