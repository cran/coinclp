% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lowlevel.R
\name{clp_set_integer}
\alias{clp_set_integer}
\alias{clp_delete_integer}
\alias{clp_integer_information}
\title{Mark columns as integer}
\usage{
clp_set_integer(model, is_integer)

clp_delete_integer(model)

clp_integer_information(model)
}
\arguments{
\item{model}{A \code{"clp_model"} object.}

\item{is_integer}{Logical vector with one entry per column.}
}
\value{
\code{clp_integer_information()} returns a logical vector (empty
  when no markers are set); the others return \code{NULL} invisibly.
}
\description{
Clp is a linear programming solver and always solves the continuous
relaxation; these markers exist so that a model can be handed on to a
branch and bound code such as Cbc.
}
\examples{
model <- clp_model()
clp_load_problem(model, 2, 0, c(0L, 0L, 0L), integer(0), numeric(0))
clp_set_integer(model, c(TRUE, FALSE))
clp_integer_information(model)
clp_free(model)
}
