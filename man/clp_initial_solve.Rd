% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solvers.R
\name{clp_initial_solve}
\alias{clp_initial_solve}
\alias{clp_initial_dual_solve}
\alias{clp_initial_primal_solve}
\alias{clp_initial_barrier_solve}
\alias{clp_initial_barrier_no_cross_solve}
\alias{clp_initial_solve_with_options}
\alias{clp_primal_simplex}
\alias{clp_dual_simplex}
\alias{clp_idiot}
\alias{clp_crash}
\title{Solve a loaded model}
\usage{
clp_initial_solve(model)

clp_initial_dual_solve(model)

clp_initial_primal_solve(model)

clp_initial_barrier_solve(model)

clp_initial_barrier_no_cross_solve(model)

clp_initial_solve_with_options(model, options)

clp_primal_simplex(model, if_values_pass = 0L)

clp_dual_simplex(model, if_values_pass = 0L)

clp_idiot(model, try_hard = 0L)

clp_crash(model, gap = 0, pivot = 0L)
}
\arguments{
\item{model}{A \code{"clp_model"} object.}

\item{options}{A \code{"clp_options"} object from \code{\link{clp_options}}.}

\item{if_values_pass}{Pass 1 to start from the values in
\code{\link{clp_set_col_solution}}, 0 otherwise.}

\item{try_hard}{Effort level for the idiot crash.}

\item{gap}{Bound gap below which variables may be flipped by the crash.}

\item{pivot}{Crash pivoting rule: 0 none, 1 simple, 2 mini iterations.}
}
\value{
An integer return code from Clp: 0 if it solved the problem,
  1 if the problem is primal infeasible, 2 if dual infeasible, and other
  values if it stopped early.  \code{clp_idiot()} returns \code{NULL}
  invisibly.  Use \code{\link{clp_status}} for the status of the model
  itself.
}
\description{
The entry points of the Clp callable library.  \code{clp_initial_solve()}
lets Clp choose the algorithm and applies presolve, which is the right
default for most problems; the others force a particular method.
\code{clp_primal_simplex()} and \code{clp_dual_simplex()} run the simplex
without presolve, which is what you want when re-solving a modified model
from a warm start.
}
\examples{
model <- clp_model()
clp_load_problem(model, 2, 1, c(0L, 1L, 2L), c(0L, 0L), c(1, 1),
                 obj = c(-1, -2), rowub = 3)
clp_set_log_level(model, 0L)
clp_initial_solve(model)
clp_objective_value(model)
clp_free(model)
}
