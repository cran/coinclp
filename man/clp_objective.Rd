% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lowlevel.R
\name{clp_objective}
\alias{clp_objective}
\alias{clp_col_lower}
\alias{clp_col_upper}
\alias{clp_row_lower}
\alias{clp_row_upper}
\alias{clp_set_objective}
\alias{clp_set_col_lower}
\alias{clp_set_col_upper}
\alias{clp_set_row_lower}
\alias{clp_set_row_upper}
\title{Bounds and objective coefficients}
\usage{
clp_objective(model)

clp_col_lower(model)

clp_col_upper(model)

clp_row_lower(model)

clp_row_upper(model)

clp_set_objective(model, value)

clp_set_col_lower(model, value)

clp_set_col_upper(model, value)

clp_set_row_lower(model, value)

clp_set_row_upper(model, value)
}
\arguments{
\item{model}{A \code{"clp_model"} object.}

\item{value}{A numeric vector with one entry per column (objective and
column bounds) or per row (row bounds).}
}
\value{
The getters return a numeric vector; the setters return \code{NULL}
  invisibly.
}
\description{
Read or replace the objective coefficients and the row and column bounds
of a model.  The getters return Clp's own values, in which an infinite
bound is \code{\link{clp_inf}()} rather than \code{Inf}.
}
\examples{
model <- clp_model()
clp_load_problem(model, 2, 0, start = c(0L, 0L, 0L),
                 index = integer(0), value = numeric(0), obj = c(1, 2))
clp_objective(model)
clp_set_objective(model, c(3, 4))
clp_objective(model)
clp_free(model)
}
