% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{clp_primal_tolerance}
\alias{clp_primal_tolerance}
\alias{clp_set_primal_tolerance}
\alias{clp_dual_tolerance}
\alias{clp_set_dual_tolerance}
\alias{clp_dual_objective_limit}
\alias{clp_set_dual_objective_limit}
\alias{clp_dual_bound}
\alias{clp_set_dual_bound}
\alias{clp_infeasibility_cost}
\alias{clp_set_infeasibility_cost}
\alias{clp_max_seconds}
\alias{clp_set_max_seconds}
\alias{clp_max_iterations}
\alias{clp_set_max_iterations}
\alias{clp_hit_max_iterations}
\alias{clp_small_element_value}
\alias{clp_set_small_element_value}
\title{Numerical tolerances and limits}
\usage{
clp_primal_tolerance(model)

clp_set_primal_tolerance(model, value)

clp_dual_tolerance(model)

clp_set_dual_tolerance(model, value)

clp_dual_objective_limit(model)

clp_set_dual_objective_limit(model, value)

clp_dual_bound(model)

clp_set_dual_bound(model, value)

clp_infeasibility_cost(model)

clp_set_infeasibility_cost(model, value)

clp_max_seconds(model)

clp_set_max_seconds(model, value)

clp_max_iterations(model)

clp_set_max_iterations(model, value)

clp_hit_max_iterations(model)

clp_small_element_value(model)

clp_set_small_element_value(model, value)
}
\arguments{
\item{model}{A \code{"clp_model"} object.}

\item{value}{The new value.}
}
\value{
The getters return a number; the setters \code{NULL} invisibly.
}
\description{
Getters and setters for the simplex tolerances and the stopping criteria.
}
\details{
\code{clp_set_max_seconds()} takes a number of seconds from now, and a
negative value removes the limit.  Clp turns that into an absolute
deadline by adding the processor time already used, so
\code{clp_max_seconds()} reads back the deadline rather than the number
that was set.
}
\examples{
model <- clp_model()
clp_set_primal_tolerance(model, 1e-8)
clp_primal_tolerance(model)
clp_set_max_iterations(model, 1000L)
clp_max_iterations(model)
clp_free(model)
}
