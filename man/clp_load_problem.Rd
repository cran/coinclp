% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lowlevel.R
\name{clp_load_problem}
\alias{clp_load_problem}
\title{Load a problem into a Clp model}
\usage{
clp_load_problem(
  model,
  ncols,
  nrows,
  start,
  index,
  value,
  collb = NULL,
  colub = NULL,
  obj = NULL,
  rowlb = NULL,
  rowub = NULL
)
}
\arguments{
\item{model}{A \code{"clp_model"} object.}

\item{ncols}{Number of columns (variables).}

\item{nrows}{Number of rows (constraints).}

\item{start}{Integer vector of length \code{ncols + 1} giving the 0-based
position in \code{index}/\code{value} at which each column starts.}

\item{index}{Integer vector of 0-based row indices, one per matrix entry.}

\item{value}{Numeric vector of matrix entries.}

\item{collb, colub}{Numeric vectors of column bounds, or \code{NULL}.}

\item{obj}{Numeric vector of objective coefficients, or \code{NULL}.}

\item{rowlb, rowub}{Numeric vectors of row bounds, or \code{NULL}.}
}
\value{
\code{NULL}, invisibly.  The model is modified in place.
}
\description{
Loads a complete linear program given by a column-major (compressed sparse
column) constraint matrix.  This is the callable library's
\code{Clp_loadProblem} and takes 0-based indices.
}
\details{
Any of the bound and objective arguments may be \code{NULL}, in which case
Clp applies its defaults: columns get \code{[0, Inf)} bounds and a zero
objective, rows get \code{(-Inf, Inf)}.
}
\examples{
# maximise 2x + 3y subject to x + y <= 4, x + 3y <= 6
model <- clp_model()
clp_load_problem(model, ncols = 2, nrows = 2,
                 start = c(0L, 2L, 4L),
                 index = c(0L, 1L, 0L, 1L),
                 value = c(1, 1, 1, 3),
                 collb = c(0, 0), colub = c(clp_inf(), clp_inf()),
                 obj = c(-2, -3),
                 rowlb = c(-clp_inf(), -clp_inf()), rowub = c(4, 6))
clp_initial_solve(model)
clp_col_solution(model)
clp_free(model)
}
\seealso{
\code{\link{clp_solve}}, which builds this representation from an
  ordinary R matrix.
}
